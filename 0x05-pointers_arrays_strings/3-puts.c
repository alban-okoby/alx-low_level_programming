#include "main.h"

/**
 * _puts: - Entry point of function
 *@str: length param 
 * description: this function prints a string, followed by a line
 * to stdout
 * Return: always 0
 */

void _puts(char *str)
{
	printf("%s\n", str);	
}
