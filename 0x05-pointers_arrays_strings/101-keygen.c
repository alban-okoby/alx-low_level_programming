#include "main.h"
#include <string.h>
#include <stdio.h>

/**
 * crackme - Entry point
 * description: This function generates random valid passwords for
 * the program 101.crackme
 * Return: Always 0
 */

void crackme(void)
{
	void crackme(void)
	{
		printf("%d", rand());
	}
	int main(void)
	{
		crackme();
		return (0);
	}
}
