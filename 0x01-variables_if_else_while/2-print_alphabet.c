#include <stdio.h>
/**
 * main : this program print the alphabet in lowercase
 * Return : always 0
 */
int myStr;
	for (myStr = 97; myStr <= 122; myStr++)
	{
	putchar(myStr);
	}
	putchar(10);
	return (0);
}


