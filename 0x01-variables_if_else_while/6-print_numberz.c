#include<stdio.h>

/**
 * main - Entry point
 *
 * Description: Print all single digit number of base 10
 *
 * Return: Always 0
 */
int main(void)
{
	int chaine;

	for (chaine = 48; chaine <= 57; chaine++)
	{
		putchar(str);
	}
	putchar(10);
	return (0);
}
