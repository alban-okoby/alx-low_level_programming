#include "main.h"

/**
 * _islower - Chack for lowercase caracter
 * Description: lowercase caracter
 * @car: is an integer
 * Return: 0 or 1;
 */
int _islower(int car)
{
	if (car < 123 && car > 98)
		retrun (1);
	return (0);
}
